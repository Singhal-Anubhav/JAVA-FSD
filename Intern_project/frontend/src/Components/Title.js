import React from 'react'

const Title = (props) => {
    return(
        <div>
            <h1 className = "title-container">Get Team Details</h1>
        </div>
    )
}

export default Title;